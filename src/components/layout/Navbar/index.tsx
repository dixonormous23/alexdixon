import styled from 'styled-components';

export const Navbar = (): React.ReactElement => {
    return (
        <nav>
            <div>
                <p>Navbar</p>
            </div>
        </nav>
    )
}